contract_name: proposal_package
schema_version: proposal_package.v1
description: Canonical contract for proposal_package.

required:
  - id

fields:
  id:
    type: string

metadata:
  created_by: MAKSIMAR_PLATFORM
  layer: portable_core
  status: draft

validation_rules:
  - id required

security_rules:
  - no implicit authority
  - execution requires approval policy
