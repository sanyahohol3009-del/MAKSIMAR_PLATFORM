"""Server runtime for family child device policy decisions.

This runtime binds only to family_child_device_control contracts.
It does not call platform APIs, open network paths, execute touch/keyboard input,
capture screens, mutate canonical truth, or control devices directly.
"""
