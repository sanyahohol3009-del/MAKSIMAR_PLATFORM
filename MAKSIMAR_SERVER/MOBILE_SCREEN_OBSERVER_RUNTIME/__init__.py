"""Server runtime for normal mobile screen observer.

This runtime is read-only and metadata/reference-only.
It does not perform screen capture, pixel decoding, platform API calls,
network operations, touch injection, keyboard injection, or device control.
"""
